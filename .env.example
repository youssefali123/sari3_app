# Copy to .env and fill in values from Supabase Dashboard → Project Settings → API,
# or ask a teammate for the dev project values.
# These are public (EXPO_PUBLIC_* is inlined into the client bundle by Expo) —
# safe to commit defaults, never put service_role / secret keys here.
EXPO_PUBLIC_SUPABASE_URL=https://ciznkxtwyrocgiwyapyj.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Iep_WTisYt47ukoDoxp_4w_wxl40ZiH
